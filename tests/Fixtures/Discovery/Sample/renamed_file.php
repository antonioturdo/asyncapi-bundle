<?php

declare(strict_types=1);

namespace Zeusi\AsyncApiBundle\Tests\Fixtures\Discovery\Sample;

// Deliberately non-PSR-4: the file name does not match the class name, so the
// FQCN can only be obtained by reading the source tokens, not the path.
final class RenamedClass {}
